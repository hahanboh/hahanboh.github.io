---
const { tags = [] } = Astro.props;
import { sitePath } from '../../lib/urls';
import { tagSlug } from '../../lib/content';
---
{tags.length > 0 && <div class="tags">{tags.map((tag: string) => <a class="tag" href={sitePath(`/tags/${tagSlug(tag)}/`)}>#{tag}</a>)}</div>}
